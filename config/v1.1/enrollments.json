[
  {
    "columnId": "sourcedId",
    "required": true,
    "format": "string",
    "description": "Id of this enrollment"
  },
  {
    "columnId": "status",
    "required": false,
    "format": "string",
    "description": "“active” | “inactive” | “tobedeleted”. A tobedeleted object may be deleted in receiving systems if it already exists"
  },
  {
    "columnId": "dateLastModified",
    "required": false,
    "format": "datetime",
    "description": "The date that this record was last modified."
  },
  {
    "columnId": "classSourcedId",
    "required": true,
    "format": "string",
    "description": "Id of the class"
  },
  {
    "columnId": "schoolSourcedId",
    "required": true,
    "format": "string",
    "description": "Id of the school"
  },
  {
    "columnId": "userSourcedId",
    "required": true,
    "format": "string",
    "description": "Id of the user (teacher or student)"
  },
  {
    "columnId": "role",
    "required": true,
    "format": "string",
    "description": "“student” | “teacher” | “parent” | “guardian” | “relative” | “aide”, “administrator”"
  },
  {
    "columnId": "primary",
    "required": false,
    "format": "boolean",
    "description": "MUST only be set to true for ONE teacher for a class."
  },
  {
    "columnId": "beginDate",
    "required": false,
    "format": "date",
    "description": "The start date for the enrollment. This date must align with the associated academic session (term) identified in the class."
  },
  {
    "columnId": "endDate",
    "required": false,
    "format": "date",
    "description": "The end date for the enrollment (exclusive). This date must align with the associated academic session (term) identified for the class."
  }
]