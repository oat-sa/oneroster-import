[
  {
    "columnId": "sourcedId",
    "required": true,
    "format": "string",
    "description":"unique id for the organization. SourcedId is used in other files and must be unique across all organizations. (For example –schools in your district)."
  },
  {
    "columnId" :"status",
    "required" : false,
    "format" :"string",
    "description" :"“active” | “inactive” | “tobedeleted”. A tobedeleted object may be deleted in receiving systems if it already exists"
  },
  {
    "columnId" :"dateLastModified",
    "required" : false,
    "format" :"date",
    "description" :"The date that this record was last modified. (format is YYYY-MM-DD)"
  },
  {
    "columnId" :"name",
    "required" :true,
    "format" :"string",
    "description" :"name of the organization"
  },
  {
    "columnId" :"type",
    "required" :true,
    "format" :"string",
    "description" :"“school” | “local” | “state” | “national”"
  },
  {
    "columnId" :"identifier",
    "required" :false,
    "format" :"string",
    "description" :"NCES ID National Center for Education Statistics) for the school / district"
  },
  {
    "columnId" :"metadata.classification",
    "required" :false,
    "format" :"string",
    "description" :"“charter” | “private” | “public”"
  },
  {
    "columnId" :"metadata.gender",
    "required" :false,
    "format" :"string",
    "description" :"“female” | “male” | “mixed”"
  },
  {
    "columnId" :"metadata.boarding",
    "required" :false,
    "format" :"boolean",
    "description" :"True if school is boarding school"
  },
  {
    "columnId" :"parentSourcedId",
    "required" :false,
    "format" :"string",
    "description" :"SourcedId of the Parent organization"
  }
]